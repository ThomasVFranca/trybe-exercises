/* Exercício 1 do dia 1 do bloco 4. */

/* 1. Crie uma constante chamada myName e atribua a ela o seu nome. */
const myName = "Thomas";

/* 2. Crie uma constante chamada birthCity e atribua a ela a sua cidade natal. */
const birthCity = "Manaus";

/* 3. Crie uma variável chamada birthYear e atribua a ela o ano em que você nasceu. */
let birthYear = 1998;

/* 4. Utilize o console.log() para imprimir as contantes e variáveis que você criou. */
console.log(myName);
console.log(birthCity);
console.log(birthYear);

/* 5. Altere o valor atribuído à constante birthYear. Faça um console.log(birthYear) novamente para ver o que acontece!. */
birthYear = 2000;
console.log(birthYear);

/* 6. Altere o valor atribuído à constante birthCity. Faça um console.log(birthCity) novamente! */
birthCity = Parintins;
console.log(birthCity);

/* Você saberia explicar por que recebemos uma mensagem de erro? */
/* Resposta: Apareceu uma mensagem de erro, pois não é possível alterar o valor de constantes depois que este já foi definido. */ 
